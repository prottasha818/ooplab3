class CurrentAccount extends Account {
    public CurrentAccount() {}

    public CurrentAccount(String accountName, String ID, double balance, String accType) {
        super(accountName, ID, balance, accType);
    }
}